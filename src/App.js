import React from 'react';
import LazyLoadedComponent from './LazyLoadedComponent';

const App = () => {
    return (
        <div>
            <h1>My React App</h1>
            <LazyLoadedComponent />
        </div>
    );
};

export default App;