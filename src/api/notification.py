async def notify_user(filename: str):
    # Here we would implement logic to notify the users, e.g., through email or WebSocket
    print(f"Notification: Your uploaded session {filename} is ready for access.")