import React, { useEffect, useState } from 'react'; const GearRecommendations = () => { const [recommendations, setRecommendations] = useState([]); useEffect(() => { fetch('/api/gear-recommendations') .then(res => res.json()) .then(data => setRecommendations(data)); }, []); return ( <div> {recommendations.map(gear => <div key={gear.id}>{gear.name}</div>)} </div> ); }; export default GearRecommendations;