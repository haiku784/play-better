import React from 'react';
import { StatusDisplay } from './styles/StatusDisplayStyles';

const StatusDisplayComponent = ({ status }) => {
  return <StatusDisplay>{status}</StatusDisplay>;
};

export default StatusDisplayComponent;
