import React, { useEffect, useState } from 'react'; import { getGearRecommendations } from './openaiIntegration'; function GearRecommendations({ userData }) { const [recommendations, setRecommendations] = useState([]); useEffect(() => { const fetchRecommendations = async () => { const data = await getGearRecommendations(userData); setRecommendations(data); }; fetchRecommendations(); }, [userData]); return <div>{recommendations.map(rec => <div key={rec}>{rec}</div>)}</div>; }