FROM ubuntu:latest

# Set working directory
WORKDIR /app

# Copy source code
COPY . .

# Install dependencies here, if needed
# RUN apt-get update && apt-get install -y <dependencies>

# Specify the command to run when the container starts
CMD ["your_start_command"]